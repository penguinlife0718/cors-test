{
  "name": "cors-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^3.5.1",
    "json-server": "^0.12.1",
    "papercss": "^1.4.1",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4",
    "styled-components": "^3.2.5"
  },
  "scripts": {
    "start": "concurrently --kill-others 'react-scripts start' 'json-server --watch database.json -p 3001'",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
